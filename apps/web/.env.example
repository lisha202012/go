# In local Vite dev the app uses the /api proxy (same origin). This value is
# used for production builds and for a remote API during development.
VITE_API_URL=http://localhost:4000/api/v1
# Same OAuth 2.0 Web client ID from Google Cloud Console
VITE_GOOGLE_CLIENT_ID=
